{
  "projectId": "1b3342fc-f88c-4c9e-ba69-ced6260b2e88",
  "shape": "package",
  "pkg": "@arach/arc",
  "globalName": "Arc",
  "buildCmd": "bun run build:lib && bunx @tailwindcss/cli@4 -i src/index.css -o .design-sync/arc-ds.tw.css --minify && cat .design-sync/arc-ds.head.css .design-sync/arc-ds.tw.css src/landing.css > .design-sync/arc-ds.css",
  "extraEntries": ["./src/design-sync-extra.tsx"],
  "tsconfig": "tsconfig.json",
  "cssEntry": ".design-sync/arc-ds.css",
  "readmeHeader": ".design-sync/conventions.md",
  "componentSrcMap": {
    "ArcDiagram": "src/components/ArcDiagram.tsx",
    "ArcDiagramIsometric": "src/iso/ArcDiagram.tsx",
    "LandingPage": "src/design-sync-extra.tsx",
    "DiagramStudio": "src/design-sync-extra.tsx",
    "DiagramEditor": null,
    "DiagramCanvas": null,
    "EditorProvider": null
  },
  "dtsPropsFor": {
    "ArcDiagram": "data: { id?: string; layout: { width: number; height: number }; nodes: Record<string, { x: number; y: number; size?: 'l' | 'm' | 's' | 'xs' }>; nodeData: Record<string, { icon: string; name: string; subtitle?: string; description?: string; color?: 'violet' | 'emerald' | 'blue' | 'amber' | 'zinc' | 'sky' }>; connectors: Array<{ from: string; to: string; fromAnchor?: 'left' | 'right' | 'top' | 'bottom'; toAnchor?: 'left' | 'right' | 'top' | 'bottom'; style?: string }>; connectorStyles?: Record<string, { color?: string; strokeWidth?: number; label?: string; dashed?: boolean }> };\n  /** Light or dark appearance. */\n  mode?: 'light' | 'dark';\n  /** Color palette theme. */\n  theme?: 'default' | 'warm' | 'cool' | 'mono';\n  /** Enable zoom/pan controls. */\n  interactive?: boolean;\n  /** Initial zoom: a number or 'fit' to auto-fit content. */\n  defaultZoom?: number | 'fit';\n  /** Cap on the auto-fit zoom level (e.g. 0.85). */\n  maxFitZoom?: number;\n  zoomLevels?: number[];\n  /** Override the bottom-left label (defaults to data.id). */\n  label?: string;\n  showArcToggle?: boolean;\n  showAutoLayout?: boolean;\n  hoverEffects?: boolean | { dim?: boolean; dimOpacity?: number; lift?: boolean; glow?: boolean; highlightEdges?: boolean };\n  onNodeHover?: (nodeId: string | null) => void;\n  className?: string;",
    "ArcDiagramIsometric": "config: { id?: string; title: string; description?: string; theme: 'dark' | 'light'; canvas: { width: number; height: number }; origin: { x: number; y: number }; cornerRadius?: number; tiers: Array<{ name: string; elevation: number; floorColor?: string; floorOpacity?: number; borderColor?: string; nodeOpacity?: number; blur?: number }>; floorSize: { width: number; depth: number }; nodes: Array<{ tier: number; x: number; y: number; width: number; depth: number; height: number; color: string; label: string; opacity?: number; blur?: number; translucent?: boolean }>; pillars?: Array<{ x: number; y: number; fromTier: number; toTier: number }> };\n  options?: { interactive?: boolean; zoomable?: boolean; showLabels?: boolean; animate?: boolean };\n  className?: string;\n  style?: React.CSSProperties;",
    "LandingPage": "/** The standalone Arc marketing homepage. Renders the full page — hero, install, architecture, docs — with no required props. */",
    "DiagramStudio": "/** The standalone Arc studio — the full diagram editor (top bar, canvas, floating toolbar, properties panel) loaded with a sample diagram. No required props. */"
  },
  "overrides": {
    "ArcDiagram": { "cardMode": "column" },
    "ArcDiagramIsometric": { "cardMode": "column" },
    "LandingPage": { "cardMode": "column", "viewport": "1280x900" },
    "DiagramStudio": { "cardMode": "single", "viewport": "1320x860" }
  }
}
