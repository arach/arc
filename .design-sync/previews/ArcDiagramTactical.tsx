// Owned preview — Arc diagram in the Tactical theme, as its own card.
import { ArcDiagramTactical } from '../../src/design-sync-extra'

/** Tactical — tactical near-black with signature amber. */
export function Tactical() {
  return <ArcDiagramTactical />
}
