// Owned preview — Arc diagram in the Workbench theme, as its own card.
import { ArcDiagramWorkbench } from '../../src/design-sync-extra'

/** Workbench — dark slate with signature intent colors. */
export function Workbench() {
  return <ArcDiagramWorkbench />
}
