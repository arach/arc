// Owned preview — the Arc studio (working editor) as a single full-bleed card.
// Imported by relative path to the design-sync entry; the story-imports policy
// redirects it to the shipped window.Arc.DiagramStudio bundle export.
import { DiagramStudio } from '../../src/design-sync-extra'

/** The Arc studio — full editor shell with a sample architecture diagram loaded. */
export function Studio() {
  return <DiagramStudio />
}
