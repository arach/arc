// Owned preview — Arc diagram in the Engineering theme, as its own card.
import { ArcDiagramEngineering } from '../../src/design-sync-extra'

/** Engineering — systematic blue (#0f62fe) on structured gray, 16px grid. */
export function Engineering() {
  return <ArcDiagramEngineering />
}
